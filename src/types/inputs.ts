export interface boxCheckedType {
    id: string,
    checked: boolean
}

export interface multipleCheckBoxType {
    id: string,
    label: string,
    value: string,
}

export interface selectDataType {
    label: string,
    value: string
}